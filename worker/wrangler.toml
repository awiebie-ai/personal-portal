name = "portfolio-headlines"
main = "src/worker.js"
compatibility_date = "2024-01-01"

# The site is frozen: this worker just serves src/snapshot.json, a static
# capture of every card's last-refreshed state. No cron triggers, no KV,
# no outbound fetches, no secrets.
